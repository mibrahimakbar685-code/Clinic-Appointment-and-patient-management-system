{
  "name": "Clinic - Webhook - Appointment Confirmed",
  "nodes": [
    {
      "parameters": {
        "httpMethod": "POST",
        "path": "appointment-confirmed",
        "responseMode": "onReceived",
        "options": {}
      },
      "name": "Webhook",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 2,
      "position": [260, 300],
      "id": "wh-confirmed-1",
      "webhookId": "appointment-confirmed"
    },
    {
      "parameters": {
        "fromEmail": "clinic@nowsherafamilyclinic.com",
        "toEmail": "={{ $json.body.to }}",
        "subject": "Your appointment is confirmed",
        "emailFormat": "text",
        "text": "=Hello,\n\nYour appointment with Dr. {{ $json.body.appointment.doctor_id }} on {{ $json.body.appointment.slot_start }} has been CONFIRMED.\n\nPlease arrive 10 minutes early.\n\n- Nowshera Family Clinic",
        "options": {}
      },
      "name": "Send Confirmation Email",
      "type": "n8n-nodes-base.emailSend",
      "typeVersion": 2.1,
      "position": [520, 300],
      "id": "wh-confirmed-2",
      "credentials": {
        "smtp": {
          "id": "REPLACE_WITH_YOUR_SMTP_CREDENTIAL_ID",
          "name": "Clinic SMTP"
        }
      }
    }
  ],
  "connections": {
    "Webhook": {
      "main": [
        [
          {
            "node": "Send Confirmation Email",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "active": false,
  "settings": {},
  "notes": "Body received: { appointment: {...}, to: patient_email }. Configure your SMTP (or swap for Gmail node) credential before activating. To improve the email, join doctor name / patient name from Supabase using an extra HTTP Request node before sending, since the backend currently only passes doctor_id."
}
